# 媒体能力入口规则

将以下规则加入目标机器的全局 `AGENTS.md`：

1. 一切图片、视频、音频请求先调用 `media-studio-orchestrator`，再由它选择并调用专用 Skill。
2. 专用 Skill 只执行被选中的步骤，不得绕过总控的健康检查、授权门禁、路由选择和验收记录。
3. 同时匹配多个媒体 Skill 时，始终以总控为协调入口，并在结果中说明实际调用链。

此文件是可移植的最小入口规则，不包含用户个人的其余全局偏好或凭据配置。
