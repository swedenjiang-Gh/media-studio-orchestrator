# 本地运行验收记录（2026-08-02）

本记录只保留能力与验证边界；不包含用户素材、绝对路径、媒体输出或凭据。

| 能力 | 实测状态 | 证据与边界 |
| --- | --- | --- |
| 视频事件扫描与关键帧抽取 | 通过 | 对一条已有竖屏 AI 视频完成 CUDA 事件扫描、事件帧与全时段覆盖帧输出。粗扫描只用于发现变化，不代表完整故事只有一个事件。 |
| 本地中文 ASR 与外置 SRT | 部分通过 | 对有明确中文对白的本地成片写出两段 UTF-8 SRT，时间码完整；ASR 将角色名转成同音字，质量门正确保留为需人工/交叉证据复核。因此 SRT 写入链路通过，字幕文案不能自动作为终稿。 |
| 非中文双语 SRT | 待实测 | 当前实际样本均为中文；实现和既有单测已覆盖编号完整性、原文保留与简中翻译异常门，但仍需一条授权的非中文对白视频进行生产验收。 |
| 人像去背景 | 基础通过 | `u2net_human_seg` 通过 CUDA 生成 RGBA PNG；标准输出对测试人像主体保留和背景去除可用。alpha matting 版本出现背景雾影，精细发丝资产仍必须人工看片。 |
| 既有图生视频案例 | 部分通过 | 已核对 8 个关键帧、逐镜提示词、8 段视频和最终成片的时序画面覆盖。该案例最终成片实际时长短于其策划目标，不能标记为完整时长验收通过。 |
| ComfyUI Wan / PuLID / Union ControlNet | 依赖通过，工作流待验收 | 实时 API 显示 Wan 1.3B、Wan I2V、UMT5、Wan VAE、PuLID 和 Union ControlNet 均可见；但尚无对应的 Canvas/API 工作流对与实际运行记录，不能声称本地 Wan 或身份/姿态链路已验收。 |

## 后续最小素材门槛

- 双语字幕：一条可处理、已授权的非中文对白视频及目标语言为简体中文。
- PuLID + Union ControlNet：6–12 张独立、已授权的人物图，姿态/构图参考与镜头说明。
- Wan I2V：已批准首帧、动作说明、帧数/fps/分辨率；先确认节点方案并保存 Canvas/API 对，再做单镜 smoke test。
